# 数据库表结构

## 服务器端 MySql 表结构
### categories 表
root@Cloud:/var/www# mysql -u root -p -e "USE user_db; DESCRIBE users;"
Enter password: 
+------------------+-------------+------+-----+-------------------+-----------------------------------------------+
| Field            | Type        | Null | Key | Default           | Extra                                         |
+------------------+-------------+------+-----+-------------------+-----------------------------------------------+
| id               | int         | NO   | PRI | NULL              | auto_increment                                |
| uuid             | varchar(36) | NO   | UNI | NULL              |                                               |
| name             | varchar(50) | NO   | MUL | NULL              |                                               |
| user_uuid        | varchar(36) | NO   | MUL | NULL              |                                               |
| created_at       | timestamp   | YES  |     | CURRENT_TIMESTAMP | DEFAULT_GENERATED                             |
| updated_at       | timestamp   | YES  |     | CURRENT_TIMESTAMP | DEFAULT_GENERATED on update CURRENT_TIMESTAMP |
+------------------+-------------+------+-----+-------------------+-----------------------------------------------+

categories | CREATE TABLE `categories` (
  `id` int NOT NULL AUTO_INCREMENT,
  `uuid` varchar(36) NOT NULL,
  `name` varchar(50) NOT NULL,
  `user_uuid` varchar(36) NOT NULL,
  `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `unique_name_per_user` (`name`,`user_uuid`),
  UNIQUE KEY `uuid` (`uuid`),
  KEY `idx_user_uuid` (`user_uuid`),
  CONSTRAINT `fk_categories_user_uuid` FOREIGN KEY (`user_uuid`) REFERENCES `user_db`.`users` (`uuid`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB AUTO_INCREMENT=19 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci

## 本地 SQLite 表结构
### categories 表
```sql
CREATE TABLE IF NOT EXISTS categories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT UNIQUE NOT NULL,
    name TEXT NOT NULL,
    user_uuid TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    synced INTEGER NOT NULL DEFAULT 0
)
```

**差异对比：**
- 本地有 `synced` 字段，服务器端没有
- 数据类型不同：服务器端使用 `varchar/timestamp/datetime`，本地使用 `TEXT/INTEGER`
- 服务器端的时间字段有自动更新机制，本地需要手动管理

### todos 表
root@Cloud:/var/www/api# mysql -u root -p -e "USE todo_app; DESCRIBE todos;"
Enter password: 
+-----------------------+--------------+------+-----+-------------------+-----------------------------------------------+
| Field                 | Type         | Null | Key | Default           | Extra                                         |
+-----------------------+--------------+------+-----+-------------------+-----------------------------------------------+
| id                    | int          | NO   | PRI | NULL              | auto_increment                                |
| uuid                  | varchar(36)  | NO   | UNI | NULL              |                                               |
| user_uuid             | varchar(36)  | NO   | MUL | NULL              |                                               |
| title                 | varchar(255) | NO   |     | NULL              |                                               |
| description           | text         | YES  |     | NULL              |                                               |
| category              | varchar(50)  | NO   |     | 未分类            |                                               |
| important             | tinyint(1)   | NO   | MUL | 0                 |                                               |
| deadline              | timestamp    | YES  | MUL | NULL              |                                               |
| recurrence_interval   | int          | NO   |     | 0                 |                                               |
| recurrence_count      | int          | NO   |     | 0                 |                                               |
| recurrence_start_date | date         | YES  |     | NULL              |                                               |
| is_completed          | tinyint(1)   | NO   |     | 0                 |                                               |
| completed_at          | timestamp    | YES  |     | NULL              |                                               |
| is_deleted            | tinyint(1)   | NO   |     | 0                 |                                               |
| deleted_at            | timestamp    | YES  |     | NULL              |                                               |
| created_at            | timestamp    | YES  | MUL | CURRENT_TIMESTAMP | DEFAULT_GENERATED                             |
| updated_at            | timestamp    | YES  |     | CURRENT_TIMESTAMP | DEFAULT_GENERATED on update CURRENT_TIMESTAMP |
+-----------------------+--------------+------+-----+-------------------+-----------------------------------------------+

todos | CREATE TABLE `todos` (
  `id` int NOT NULL AUTO_INCREMENT,
  `uuid` varchar(36) NOT NULL,
  `user_uuid` varchar(36) NOT NULL,
  `title` varchar(255) NOT NULL,
  `description` text,
  `category` varchar(50) NOT NULL DEFAULT '未分类',
  `important` tinyint(1) NOT NULL DEFAULT '0',
  `deadline` timestamp NULL DEFAULT NULL,
  `recurrence_interval` int NOT NULL DEFAULT '0',
  `recurrence_count` int NOT NULL DEFAULT '0',
  `recurrence_start_date` date DEFAULT NULL,
  `is_completed` tinyint(1) NOT NULL DEFAULT '0',
  `completed_at` timestamp NULL DEFAULT NULL,
  `is_deleted` tinyint(1) NOT NULL DEFAULT '0',
  `deleted_at` timestamp NULL DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uuid` (`uuid`),
  KEY `idx_deadline` (`deadline`),
  KEY `idx_created_at` (`created_at`),
  KEY `idx_important` (`important`),
  KEY `idx_user_uuid_category` (`user_uuid`,`category`),
  KEY `idx_user_uuid_completed` (`user_uuid`,`is_completed`),
  KEY `idx_user_uuid_deleted` (`user_uuid`,`is_deleted`),
  CONSTRAINT `fk_todos_user_uuid` FOREIGN KEY (`user_uuid`) REFERENCES `user_db`.`users` (`uuid`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB AUTO_INCREMENT=11 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci

### todos 表
```sql
CREATE TABLE IF NOT EXISTS todos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT UNIQUE NOT NULL,
    user_uuid TEXT NOT NULL,
    title TEXT NOT NULL,
    description TEXT,
    category TEXT NOT NULL DEFAULT '未分类',
    important INTEGER NOT NULL DEFAULT 0,
    deadline TEXT,
    recurrence_interval INTEGER NOT NULL DEFAULT 0,
    recurrence_count INTEGER NOT NULL DEFAULT 0,
    recurrence_start_date TEXT,
    is_completed INTEGER NOT NULL DEFAULT 0,
    completed_at TEXT,
    is_deleted INTEGER NOT NULL DEFAULT 0,
    deleted_at TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    synced INTEGER NOT NULL DEFAULT 0
)
```

**差异对比：**
- 本地有 `synced` 字段，服务器端没有
- 数据类型不同：服务器端使用 `varchar/text/tinyint/datetime/timestamp/date/int`，本地统一使用 `TEXT/INTEGER`
- 服务器端的时间字段有自动更新机制，本地需要手动管理


### users 表
root@Cloud:~# mysql -u root -p -e "USE user_db; DESCRIBE users;"
Enter password: 
+------------+------------------------------------+------+-----+-------------------+-------------------+
| Field      | Type                               | Null | Key | Default           | Extra             |
+------------+------------------------------------+------+-----+-------------------+-------------------+
| id         | int                                | NO   | PRI | NULL              | auto_increment    |
| uuid       | varchar(36)                        | NO   | UNI | NULL              |                   |
| username   | varchar(50)                        | NO   | UNI | NULL              |                   |
| password   | varchar(255)                       | NO   |     | NULL              |                   |
| email      | varchar(100)                       | NO   | UNI | NULL              |                   |
| phone      | varchar(20)                        | YES  | UNI | NULL              |                   |
| avatar_url | varchar(255)                       | YES  |     | NULL              |                   |
| created_at | timestamp                          | YES  |     | CURRENT_TIMESTAMP | DEFAULT_GENERATED |
| last_login | timestamp                          | YES  |     | NULL              |                   |
| api_key    | varchar(64)                        | YES  | UNI | NULL              |                   |
| status     | enum('active','inactive','banned') | YES  |     | active            |                   |
+------------+------------------------------------+------+-----+-------------------+-------------------+

users | CREATE TABLE `users` (
  `id` int NOT NULL AUTO_INCREMENT COMMENT '用户唯一标识',
  `uuid` varchar(36) NOT NULL,
  `username` varchar(50) NOT NULL COMMENT '登录用户名',
  `password` varchar(255) NOT NULL COMMENT '加密存储的密码',
  `email` varchar(100) NOT NULL COMMENT '用户电子邮箱',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号码',
  `avatar_url` varchar(255) DEFAULT NULL COMMENT '头像URL',
  `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP COMMENT '账号创建时间',
  `last_login` timestamp NULL DEFAULT NULL COMMENT '最后登录时间',
  `api_key` varchar(64) DEFAULT NULL COMMENT 'API访问密钥',
  `status` enum('active','inactive','banned') DEFAULT 'active' COMMENT '账号状态',
  PRIMARY KEY (`id`),
  UNIQUE KEY `username` (`username`),
  UNIQUE KEY `email` (`email`),
  UNIQUE KEY `uuid` (`uuid`),
  UNIQUE KEY `phone` (`phone`),
  UNIQUE KEY `api_key` (`api_key`)
) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci

// curl -i -X POST 'http://127.0.0.1:8080/api/v1/auth?action=register' -H 'Content-Type: application/json' -d '{"username":"sakurakugu","email":"171350650@qq.com","password":"040130Pyw"}'